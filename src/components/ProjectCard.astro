---
import TagBadges from './TagBadges.astro';

interface Props {
  nome: string;
  descricao: string;
  tags: string[];
  url: string;
  maxTags?: number;
  headingLevel?: 'h2' | 'h3';
}

const { nome, descricao, tags, url, maxTags, headingLevel = 'h2' } = Astro.props;
const Titulo = headingLevel;
---

<div class="card bg-base-100 card-animate">
  <div class="card-body p-6 flex flex-col">
    <p class="card-kicker mb-2">Projeto</p>

    <div class="flex items-start justify-between mb-3">
      <Titulo class="card-title text-xl font-semibold flex-1">{nome}</Titulo>
    </div>

    <p class="text-base-content/70 text-sm leading-relaxed mb-4">
      {descricao}
    </p>

    <div class="mb-6">
      <TagBadges tags={tags} maxTags={maxTags} />
    </div>

    <div class="card-actions justify-end mt-auto">
      <a href={url} target="_blank" rel="noopener noreferrer" class="card-cta">
        Ver projeto<span class="cta-arrow" aria-hidden="true">→</span>
      </a>
    </div>
  </div>
</div>
